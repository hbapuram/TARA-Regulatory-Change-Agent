[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tara"
version = "0.1.0"
description = "TARA — Tax, Assets & Residency Advisor. Cross-border individual asset compliance, built for TechIreland National AI Challenge 23 (Regulatory Change-to-Action Agent)."
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "mcp>=2.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
llm = ["openai>=1.50", "python-dotenv>=1.0"]

[tool.setuptools.packages.find]
include = ["tara*"]

[project.scripts]
tara = "tara.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
