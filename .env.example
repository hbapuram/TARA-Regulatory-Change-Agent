# Copy to .env and fill in. Never commit the real .env — it's gitignored.
# Only needed for Phase 2 (the OpenAI orchestrator). Everything in Phase 1
# (SURVEY, LEGEND, ALMANAC, COMPASS, PLOT, COURSE, ANCHOR, ATLAS, the MCP
# server, and `tara demo --direct`) runs with no key at all.
OPENAI_API_KEY=
